#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 *
 * Return: the result
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
