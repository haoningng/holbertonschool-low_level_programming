#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: integer to be acted upon
 *
 * Return: the absolute value of an integer
 */
int _abs(int i)
{
	return (abs(i));
}

